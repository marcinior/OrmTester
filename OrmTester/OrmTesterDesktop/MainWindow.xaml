<Window x:Class="OrmTesterDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrmTesterDesktop"
        xmlns:p="clr-namespace:OrmTesterDesktop.Properties"
        mc:Ignorable="d"
        Title="MainWindow" Height="520" Width="1600">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <Label Content="{x:Static p:Resources.RelationshipType}"/>
            <CheckBox x:Name="ManyToMany" Content="{x:Static p:Resources.ManyToMany}"/>
            <StackPanel Margin="15,0,0,0" IsEnabled="{Binding ElementName=ManyToMany, Path=IsChecked}">
                <CheckBox x:Name="ManyToManyBulk" Content="{x:Static p:Resources.Bulk}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=ManyToManyBulk, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
                <CheckBox x:Name="ManyToManySingle" Content="{x:Static p:Resources.Single}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=ManyToManySingle, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
            </StackPanel>
            <CheckBox x:Name="OneToMany" Content="{x:Static p:Resources.OneToMany}"/>
            <StackPanel Margin="15,0,0,0" IsEnabled="{Binding ElementName=OneToMany, Path=IsChecked}">
                <CheckBox x:Name="OneToManyBulk" Content="{x:Static p:Resources.Bulk}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=OneToManyBulk, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
                <CheckBox x:Name="OneToManySingle" Content="{x:Static p:Resources.Single}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=OneToManySingle, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
            </StackPanel>
            <CheckBox x:Name="OneToOne" Content="{x:Static p:Resources.OneToOne}"/>
            <StackPanel Margin="15,0,0,0" IsEnabled="{Binding ElementName=OneToOne, Path=IsChecked}">
                <CheckBox x:Name="OneToOneBulk" Content="{x:Static p:Resources.Bulk}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=OneToOneBulk, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
                <CheckBox x:Name="OneToOneSingle" Content="{x:Static p:Resources.Single}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=OneToOneSingle, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
            </StackPanel>
            <CheckBox x:Name="None" Content="{x:Static p:Resources.None}"/>
            <StackPanel Margin="15,0,0,0" IsEnabled="{Binding ElementName=None, Path=IsChecked}">
                <CheckBox x:Name="NoneBulk" Content="{x:Static p:Resources.Bulk}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=NoneBulk, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
                <CheckBox x:Name="NoneSingle" Content="{x:Static p:Resources.Single}"/>
                <TextBox PreviewTextInput="NumberValidationTextBox" IsEnabled="{Binding ElementName=NoneSingle, Path=IsChecked}" Text="{x:Static p:Resources.Repetitions}"/>
            </StackPanel>
            <StackPanel>
                <Button Margin="0,15,0,0" Content="{x:Static p:Resources.ExecuteTest}" Click="ExecuteTestsButton_Click"/>
                <Button Margin="0,30,0,0" Content="{x:Static p:Resources.ExportToFile}" Click="ExportToFileButton_Click"/>
                <Button Margin="0,15,0,0" Content="{x:Static p:Resources.ExportToCSV}" Click="ExportToCsvButton_Click"/>
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Row="0" Grid.Column="1">
            <DataGrid ItemsSource="{Binding TestResults}" Width="Auto" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{x:Static p:Resources.TestName}" Binding="{Binding TestName}"/>
                    <DataGridTextColumn Header="{x:Static p:Resources.EfCoefficentOfVariation}" Binding="{Binding EfCoefficentOfVariation}"/>
                    <DataGridTextColumn Header="{x:Static p:Resources.EfStandardDeviation}" Binding="{Binding EfStandardDeviation}"/>
                    <DataGridTextColumn Header="{x:Static p:Resources.EfAverage}" Binding="{Binding EfAverage}"/>
                    <DataGridTextColumn Header="{x:Static p:Resources.Difference}" Binding="{Binding Difference}"/>
                    <DataGridTextColumn Header="{x:Static p:Resources.NHibernateAverage}" Binding="{Binding NHibernateAverage}"/>
                    <DataGridTextColumn Header="{x:Static p:Resources.NHiberanteStandardDeviation}" Binding="{Binding NHiberanteStandardDeviation}"/>
                    <DataGridTextColumn Header="{x:Static p:Resources.NHibernateCoefficentOfVariation}" Binding="{Binding NHibernateCoefficentOfVariation}"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</Window>
