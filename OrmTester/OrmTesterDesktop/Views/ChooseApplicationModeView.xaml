<Window x:Class="OrmTesterDesktop.Views.ChooseApplicationModeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p="clr-namespace:OrmTesterDesktop.Properties"
        xmlns:local="clr-namespace:OrmTesterDesktop.Views"
        mc:Ignorable="d"
        Title="ChooseApplicationModeView" Height="250" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.ColumnSpan="2" Content="{x:Static p:Resources.AppMode}" HorizontalContentAlignment="Center"/>
        <Button Grid.Row="1" Grid.Column="0" Content="{x:Static p:Resources.LoadFile}" Click="LoadButton_Click"/>
        <Button Grid.Row="1" Grid.Column="1" Content="{x:Static p:Resources.StartNewTest}" Click="StartNewTestButton_Click"/>
    </Grid>
</Window>
